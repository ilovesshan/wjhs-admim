import { defineStore } from "pinia";

const rootStore = defineStore("rootStore", {
  state() {
    return {
      name: "ilovesshan"
    }
  },
  getters: {},
  actions: {}
});


export default rootStore;